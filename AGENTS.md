# 작업 지침

## 최신 접속 주소 자동 확인 구현 규칙

도메인이 자주 변경되는 확장에 최신 접속 주소 확인 기능을 추가하거나 수정할 때는 다음 우선순위와 동작을 기본값으로 사용한다.

1. 사용자가 입력한 수동 전체 URL
2. 유효 기간 안의 자동 탐색 캐시
3. 공식 주소 안내 페이지 또는 공식 API의 탐색 결과
4. 현재 주소나 빌드 기본 주소의 HTTP 리다이렉트 결과
5. 유효 기간이 지난 마지막 정상 캐시
6. 빌드에 포함된 기본 주소

- 수동 설정은 도메인 번호가 아닌 `https://...` 형태의 전체 Base URL을 받는다. 수동 URL이 설정되어 있는 동안에는 자동 탐색 결과로 덮어쓰지 않는다. 값을 비우면 자동 모드로 복귀한다.
- 수동 URL은 HTTPS, 기본 포트, 루트 경로, 쿼리·사용자 정보 없음 여부를 검증한다. 자동 탐색 주소는 여기에 사이트별 허용 호스트 패턴까지 검증한다.
- 안내 페이지와 리다이렉트에서 얻은 정상 주소는 같은 자동 캐시에 저장한다. 실패 시 마지막 정상 주소를 유지하며, 일시적인 안내 사이트 장애만으로 작동 중인 주소를 폐기하지 않는다.
- 자동 캐시는 기본 12시간, 실패 후 재시도 제한은 기본 15분, 주소 확인 네트워크 타임아웃은 기본 8초로 둔다. 동시 요청이 주소 확인을 중복 실행하지 않도록 잠금 또는 동등한 동시성 제어를 적용한다.
- 주소 확인은 별도 클라이언트를 사용하고 리다이렉트 fallback 확인에서는 자동 리다이렉트를 끈다. 메인 요청 인터셉터를 재귀 호출하는 구조를 만들지 않는다.
- 인터셉터는 해당 사이트의 자동 호스트와 사용자가 지정한 수동 호스트만 재작성한다. 이미지 CDN, 광고 서버 등 외부 호스트 요청은 변경하지 않는다.
- 요청 URL을 바꿀 때는 scheme, host, port를 함께 맞추고 사이트가 요구하는 `Referer`와 `Origin`도 최종 주소 기준으로 갱신한다.
- 설정 화면에는 현재 수동 또는 자동 주소와 자동 탐색 출처를 표시한다. 기존 기본 주소가 저장돼 있던 경우 이를 사용자의 명시적 수동 설정으로 오인하지 않도록 마이그레이션한다.
- 사이트 구조 때문에 이 규칙을 그대로 적용할 수 없다면 임시 우회 대신 이유와 대체 복구 순서를 코드 주석이나 문서에 남긴다.
- 기능 변경 시 해당 확장의 `extVersionCode`를 올리고, 아래 Mihon Android 런타임 검증을 마친 뒤에만 푸시한다.

## Mihon Android 런타임 검증

파서, 도메인, 회차 목록, 뷰어, WebView, 광고 확인, Cloudflare 또는 이미지 API 인증 로직을 변경하는 경우에는 버전 업이나 배포 전에 실제 Mihon Android 런타임으로 검증한다.

확장 소스나 동작을 변경한 경우에는 원격 푸시 전에 해당 확장의 `extVersionCode`를 올린다. 버전 업데이트 없이 변경 사항을 푸시하지 않는다.

- 기존 Android SDK·에뮬레이터가 있으면 재사용한다. 새 Emulator 또는 시스템 이미지를 설치해야 하면 먼저 사용자에게 알리고 승인받는다.
- 변경한 확장 APK를 Mihon에 설치하고 신뢰한 뒤, 대상 소스가 목록에 나타나는지 확인한다.
- 대표 작품에서 목록, 상세, 회차 목록, 뷰어 이미지 표시까지 확인한다.
- 회차가 많은 작품은 사이트의 총 회차 수와 Mihon의 회차 수가 일치하고 `Missing chapters`가 없는지 확인한다.
- 광고 확인, Cloudflare, 이미지 API 또는 서명 로직을 건드렸다면 실제 뷰어에서 `NTK image API failed`, `WebView timed out` 같은 오류가 없는지도 로그와 화면으로 확인한다.
- 런타임 환경이 없어서 검증하지 못했다면 이를 명확히 보고한다. 컴파일·HTTP 응답만으로 실제 앱에서 해결됐다고 말하거나 버전만 올리지 않는다.

문서만 수정하거나 실행 경로와 무관한 단순 변경에는 이 검증을 요구하지 않는다.
