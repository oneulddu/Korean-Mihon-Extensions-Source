# 공식 주소 안내 복구 (2026-09-09)

## 변경

- 짭툰 1.4.15: 공식 안내 도메인의 이동을 따라가며, 안내 HTML이 자바스크립트로 읽는 `data/domain.json`의 `domain` 값을 우선 사용한다. `.net` 접속 장애에는 확인된 `.live`, `.cc` 안내 주소를 사용한다. JSON을 읽지 못하면 기존 HTML 주소 추출과 콘텐츠 도메인의 리다이렉트 순으로 복구한다.
- 늑대닷컴 1.4.12: `a14c.com` 장애 시 본문 사이트가 직접 연결하는 공개 공식 채널 `t.me/s/wfwf_com`을 조회한다. 해당 채널의 게시글 번호를 내림차순으로 확인하고 전달 게시글·외부 채널·링크 미리보기는 주소 후보에서 제외한다.
- 안내 탐색은 총 8초 안에서 요청별 최대 3초를 배정한다. 허용된 안내 주소 사이의 이동만 따르며, 콘텐츠 주소는 HTTPS·기본 포트·루트 경로·사이트별 호스트 검증을 통과해야 한다.
- 수동 URL 우선순위, 12시간 자동 캐시, 실패 후 15분 재시도 제한은 유지한다. 업데이트 후 한 번 캐시의 확인 시각만 만료시켜 새 탐색을 실행하고, 마지막 정상 URL은 실패 시 복구용으로 보존한다.
- 기존 소스 ID, 저장된 작품·회차 URL, 파서 동작은 유지한다. 완료된 짭툰·늑대닷컴 커밋은 공식 주소 탐색 수정만 포함한다.

## 근거

- 짭툰: `https://xn--kd6b44m.net/`의 이동 대상은 `https://xn--kd6b44m.live/`이며 안내 페이지의 canonical은 `.cc`다. `/js/config.js`가 `data/domain.json`을 지정하고 `/js/api.js`는 JSON의 `domain`을 표시한다. 확인한 응답은 `https://www.jjaptoon008.com`이었다.
- 늑대닷컴: `https://wfwf494.com/`의 주소안내 메뉴가 `https://t.me/s/wfwf_com`으로 연결된다. 공식 채널 게시글 149·150은 `https://wfwf494.com`을 안내했다. 두 번째 서비스 `wftoon227.com`은 현재 확장의 자동 주소 후보로 사용하지 않는다.

## 검증

- 두 모듈의 `spotlessCheck`, `testDebugUnitTest`, `assembleRelease` 통과. 단위 테스트 17개(신규 11개), 실패 0개.
- 안내 이동 → JSON 조회, 첫 안내 접속 실패, HTML 복구, 외부 도메인과 순환 리다이렉트 거부, 잘못된 JSON URL 거부, 탐색 시간 초과, 공식 채널의 최신 게시글 선택을 검증했다.
- 기존 API 35 에뮬레이터의 Mihon에 APK를 설치하고 신뢰한 뒤 수동 URL 없이 확인했다. 목록·상세·회차 목록·실제 뷰어 이미지가 표시됐고 아래 대표 작품에 `Missing chapters` 표시는 없었다.

| 소스 | 작품 | Mihon / 사이트 총 회차 | 확인한 뷰어 |
|---|---|---:|---|
| 짭툰 | 나 혼자만 레벨업 | 202 / 202 | 외전 21화, 69개 이미지 중 첫 이미지 표시 |
| 늑대닷컴 웹툰 연재 | 정령사: 나타르 전기 | 878 / 878 | 540화, 62개 이미지 중 첫 이미지 표시 |
| 늑대닷컴 웹툰 완결 | 나 혼자만 레벨업 | 203 / 203 | 외전 21화, 168개 이미지 중 첫 이미지 표시 |
| 늑대닷컴 만화책 | 원피스 | 1,200 / 1,200 | 1184화, 12개 이미지 중 첫 이미지 표시 |

늑대닷컴의 회차 수에는 기존 구현의 연속 번호 복구가 포함된다. 모든 과거 회차의 이미지와 제목을 개별 검증했다는 뜻은 아니다. 캐시 주소와 설정의 탐색 출처도 짭툰 공식 포털/API, 늑대닷컴 공식 텔레그램 안내로 확인했다.

## Xtoon 후속 수정 — 런타임 접속 검증 대기

- 새 콘텐츠 사이트가 직접 연결하는 공식 안내 `https://xn--vg1b002axlc8ts.com/`과 공식 공개 채널 `https://t.me/s/newxtoon`을 사용한다. 확인한 주소는 `https://newxtoon1.com`이다. 이전에 등록된 안내 도메인 `xn--9t4b31dr7o.com`과는 다른 주소다.
- 새 `/comics`, `/search?q=...`, `/comics/{id}/chapters/{id}` 구조에 맞춰 목록·검색·상세·회차·이미지 파서를 변경했다. 필터는 새 사이트에서 확인한 값만 사용하며, 실제 다음 페이지 링크가 있을 때만 계속 조회한다.
- 회차는 상세의 `section#chapters` 첫 목록과 `data-chapters-url`의 JSON 페이지를 끝까지 합친다. 중복·순환·부분 목록을 감지하며 번호나 회차 URL을 임의로 생성하지 않는다. 뷰어는 `img[data-reader-image]`와 부모의 `data-image-position`을 사용하고 이미지 오류 처리 코드의 총 개수 힌트로 마지막 이미지 누락도 검사한다.
- 소스 이름·ID·언어·versionId는 유지한다. 옛 `/comic/{id}`는 새 데이터베이스와 ID 대응이 입증되지 않아 같은 번호로 치환하지 않는다. 저장된 제목의 유일한 정확 일치 검색 결과를 상세에서 재확인하고, 작가 정보가 있으면 교집합을 확인한 뒤 매핑을 저장한다. 이름이 바뀌거나 중복·불일치가 있으면 Mihon의 소스 이전을 안내한다. 옛 `/chapter/{id}`는 작품 문맥이 없어 새 회차로 추정하지 않고 목록 새로고침을 안내한다. 과거 읽음 기록까지 자동 이전된다는 보장은 없다.
- 수동 URL, 자동 캐시, 주소 재시도 우선순위를 유지한다. 이미지 CDN 요청은 주소 재작성 대상에서 제외하고 실제 회차 URL을 Referer로 보낸다.

### 확보한 근거와 검증 범위

2026-09-09의 공개 원본 HTML과 JSON을 Jina Reader로 수집해 개발용 근거로 사용했다. 확장 런타임에는 Jina나 프록시 경로를 넣지 않았다. 테스트 자료는 광고 카드 구분·파서 속성·회차 데이터만 남기고 폼과 세션 토큰 등을 제거했다.

- 인기 목록의 24개 카드 중 1개는 광고다. 실제 작품 23개와 다음 페이지 링크를 확인했다.
- 화산귀환(`/comics/1876`)의 첫 20개 + API 2~8페이지 각 20개 + 마지막 18개 = 178개다. 뷰어의 전체 회차 178개와 ID 집합·순서까지 일치한다. 마지막 API는 `has_more=false`, `next_page=null`이다.
- 1화(`/comics/1876/chapters/139884`)는 본문 이미지 146개다. 첫 이미지는 실제 회차 Referer를 사용한 직접 GET이 HTTP 200이며 690×1600 WebP로 디코딩됐다.
- 화산귀환 검색에서 정확한 제목 일치 1건을 확인했다. 검색 결과에 없는 다음 페이지를 추측하지 않는다.
- 모듈 `spotlessCheck`, `testDebugUnitTest`, `assembleRelease` 통과, 테스트 17개 실패 0개다. 회차 전체 수집, 파서 범위, 페이지 진행, 도메인 검증, 안내 장애 복구, 이미지 순서·누락을 테스트한다.

기존 API 35 Android 에뮬레이터에 수정 APK를 설치했고 Mihon에서 Xtoon 소스 등록, 수동 주소가 비어 있는 상태, 공식 안내를 통한 `newxtoon1.com` 자동 저장과 출처 표시를 확인했다. 그러나 목록 요청은 `ConnectException`으로 실패했고 WebView에서도 `net::ERR_CONNECTION_RESET`이 발생했다. **실제 Mihon 목록·상세·회차·뷰어 성공 검증은 아직 완료하지 못했다.** 저장소 규칙에 따라 `extVersionCode`는 6을 유지하고 Xtoon 수정의 커밋·푸시는 보류한다. 연결 가능한 환경에서 런타임 검증을 완료한 뒤 버전 인상과 배포를 진행해야 한다.

Toonkor는 확인된 공식 안내 주소가 없어 이번 브랜치에 포함하지 않는다.

## 실제 배포 확인 (2026-09-09)

사용자의 배포 요청으로 검증 완료된 주소 복구 커밋 `477325513fd35b406335237cbf37be47c787b749`에서 짭툰 1.4.15와 늑대닷컴 1.4.12를 배포했다.

- GitHub Actions: [34280561275](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/actions/runs/34280561275), `success`.
- 배포 저장소 `repo` 브랜치 커밋: `19ec5a8db007bb4f41457750648e5120e15b534b`.
- 공개 인덱스의 두 버전·파일명이 일치하고 다른 확장의 인덱스 항목은 그대로임을 확인했다.
- 공개 APK 두 개를 다운로드해 패키지명과 versionCode(15/12), versionName(1.4.15/1.4.12), 서명 인증서 SHA-256이 기존 저장소 값과 일치함을 확인했다.

이 배포는 공식 주소 탐색 복구 두 확장에 한정한다. 전체 정비 및 Xtoon 후속 수정은 포함하지 않았다. 미검증 항목을 포함할지에 대한 범위 확인은 별도로 대기 중이다.

## 전체 배포 지시

위 보류 기록 이후 사용자가 미검증 항목까지 포함해 배포를 먼저 진행하도록 지시했다. Xtoon 버전은 7로 올리고 전체 정비와 함께 배포한다. 실제 접속 미검증 상태는 유지하며, 이번 배포 예외의 근거와 범위는 [소스 정비와 검증 기록](CODE_REVIEW.md)에 남긴다.

### 전체 배포 완료 (2026-09-09)

- 소스 커밋: `6143ca507689bee2043c2e65638feb78edd048cb` (`feature/source-consistency-0908`).
- GitHub Actions [34282416688](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/actions/runs/34282416688): `success`.
- 배포 저장소 `repo` 커밋: `4d5d6ae198eeb3f02d653950e86357224497d4e6`.
- 공개 인덱스의 9개 버전 상승, APK 9개의 패키지명·버전·서명, 기존 14개 소스 ID 유지를 모두 확인했다.
- 배포 성공은 위에 기록한 사이트 접속 실패의 해결을 뜻하지 않는다. Xtoon·Toonkor·NTK Webtoon의 미검증 상태와 일부 작품의 회차 번호 공백은 후속 확인 대상으로 남는다.


## 2026-09-18 주소 회전 복구 — 늑대닷컴 1.4.14

늑대닷컴 콘텐츠 사이트의 주소안내 메뉴는 이제 `https://t.me/s/wftoon`을 연결한다. 자체 게시글 `wftoon/44`가 안내하는 주소는 `https://wfwf501.com`이다. 옛 `wfwf_com`은 게시글 없는 채널 화면으로 이동하므로 새 채널과 게시글 식별자로 변경했다. 전달 게시글과 별도 서비스 `wftoon229.com`은 사용하지 않는다.

오래된 주소의 `a.main-btn` 이동 안내도 처리한다. 일반 HTTP 200만으로 오래된 주소를 정상으로 판정하지 않으며, 안내 버튼이나 실제 목록·상세 마크업을 확인한다. 안내 URL은 사용자 정보·포트·경로·쿼리·fragment를 포함한 전체 값을 검증한 뒤 기본 포트의 HTTP 링크만 HTTPS로 승격한다. Jsoup의 `absUrl`이 사용자 정보를 제거할 수 있어 안내 링크는 원래 `href`를 검사한다.

새 `AutomaticDomainInterceptor`는 이를 선택한 확장에만 적용한다. 자동 사이트 요청의 연결 오류 또는 502·503·504에는 만료 전 캐시도 한 번 재탐색한다. 실패 후 15분 동안 중복 재탐색을 제한하고 마지막 정상 주소를 보존한다. 이전에 진행 중이던 탐색은 세대 번호로 무효화하며 동시 요청은 새 탐색 하나를 공유한다. 정상 리다이렉트와 복구 재요청의 최종 주소를 같은 캐시에 기록한다. 취소된 요청·수동 주소·외부 CDN·일반 404는 재탐색 대상이 아니다.

늑대닷컴은 업데이트 후 캐시의 확인 시각만 한 번 만료시킨다. 12시간 정상 캐시와 수동 URL 우선순위는 유지한다. 기존 회차 생성·파서 및 소스 ID는 변경하지 않았다.

### 검증

기존 API 35 에뮬레이터의 Mihon에 서명된 수정 APK를 설치하고 신뢰했다. 수동 주소 없이 세 소스 모두 `wfwf501.com`을 자동 저장하고 실제 목록·검색·상세·뷰어 이미지를 표시했다.

| 소스 | 작품 | Mihon / 사이트 총 회차 | 실제 뷰어 |
|---|---|---:|---|
| 웹툰 연재 | 정령사: 나타르 전기 | 878 / 878 | 540화, 62페이지 |
| 웹툰 완결 | 나 혼자만 레벨업 | 203 / 203 | 외전 21화, 168페이지 |
| 만화책 | 원피스 | 1,200 / 1,200 | 1184화, 12페이지 |

세 작품의 상세에 `Missing chapters` 표시가 없었다. 회차 수에는 기존 구현의 연속 번호 복구가 포함되며 모든 과거 회차 이미지를 개별 검증한 것은 아니다. 화면 증거는 `/tmp/mihon-fixes-0918/wolf-*-detail.png`, `/tmp/mihon-fixes-0918/wolf-*-viewer.png`에 보관했다.

### 짭툰 접속 검증 대기

첫 안내 API 실패 후 과거 HTML 주소를 즉시 반환하는 동작을 수정해 보조 안내 API까지 확인하도록 준비했다. 전체 주소 검증, 자동 주소 복구와 회귀 테스트도 통과했다. 그러나 이 환경에서는 공식 `.net`·`.live`·`.cc` 안내와 `www.jjaptoon008.com`이 TLS 연결 단계에서 재설정되며, Mihon에서도 같은 접속 실패가 발생했다. 직접 연결 성공 또는 목록·상세·회차·뷰어 성공으로 보고하지 않는다. 런타임 검증 예외 승인이 없는 동안 짭툰의 버전 인상과 커밋·푸시는 보류한다.


## main 반영 및 실제 APK 배포 (2026-09-18)

작업 브랜치에만 머지했던 상태를 [PR #29](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/pull/29)로 바로잡았다. 이미 공개 배포된 이전 소스와 이번 굿툰·늑대닷컴 수정을 `main`에 반영했고, main 푸시가 자동 배포를 실행했다.

- 소스 머지 커밋: `8b0945c95c4c2e2c515b9dcd98b0f26800d52918`.
- [자동 배포 실행 35271782999](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/actions/runs/35271782999): `push`, `main`, `success`.
- 배포 저장소 `repo` 커밋: `7fb76eeed9481588fcb8bb2345fef06f4a2acbb2`.
- 공개 인덱스의 굿툰 1.4.2·늑대닷컴 1.4.14를 확인했다. 나머지 8개 확장 항목은 배포 전과 동일하며 10개 확장·15개 소스 ID를 유지했다.
- 공개 APK 10개를 직접 다운로드해 패키지명·버전·서명을 확인했다. 모든 서명 인증서 SHA-256은 기존 `b25af02d178fad20ebe739e59336f2ae5e307dcd1375418278e752dba03497cb`와 일치한다.
- 굿툰 APK SHA-256: `dd8b04aaf8623547f44a05f4c7d3614b6d95382b9480f2faa4bdd1480dd32126`.
- 늑대닷컴 APK SHA-256: `ead70933b49fcd1ae65ebb3946b60b6320401b8187e43896e344d4a04a6fd5bd`.

짭툰의 이번 미검증 수정은 포함하지 않았고 공개 버전은 기존 1.4.16을 유지한다. 보류 수정은 `feature/jjaptoon-runtime-0918` 작업 트리에 보존했다.

### 7월 27일 작업 배포 감사

7월 27일 작업은 당시 main에 반영되고 APK도 공개 배포됐다. [당일 최종 배포 커밋](https://github.com/oneulddu/Korean-Mihon-Extensions/commit/117a7c8f2e5faac5389e9f362a96736ebdbfdef6)의 인덱스를 직접 대조했다.

| 확장 | 당일 공개 버전 | 배포 실행 |
|---|---|---|
| 늑대닷컴 | 1.4.10 | [30245669649](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/actions/runs/30245669649) |
| 툰코르 | 1.4.11 | [30246584323](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/actions/runs/30246584323) |
| X툰 | 1.4.6 | [30247404215](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/actions/runs/30247404215) |
| 짭툰 | 1.4.11 | [30248478871](https://github.com/oneulddu/Korean-Mihon-Extensions-Source/actions/runs/30248478871) |

늑대닷컴 최초 자동 실행 `30245209686`은 실패했으나 같은 날 수동 실행 `30245669649`가 성공하고 배포 커밋 `645af90bb0457fd15b7fd5f3e845f3734bd2491c`을 생성했다. 이를 미배포 상태로 판단하지 않는다.
